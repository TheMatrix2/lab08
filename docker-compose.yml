version: '3'

services:
  server:
    build: ./server
    ports:
      - "8080:8080"
  client:
    build: ./client
    network_mode: host
    depends_on:
      - server
#    restart: on-failure

#networks:
#  net:
#    driver: bridge
#    ipam:
#      config:
#       - subnet: 192.168.77.0/24